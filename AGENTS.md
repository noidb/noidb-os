# 현재 작업 규칙 — 단일 폴더 통합 (2026-09-15)

- 사용자의 최신 지시에 따라 모든 업무의 실제 소스 수정 위치는 `E:\노이드비AI` 하나다.
- 신규 worktree 생성과 업무별 worktree 분할을 하지 않는다.
- 아래 2026-09-10 연결표와 worktree별 수정 위치는 과거 기록이다. 현재 작업 위치로 사용하지 않는다.
- 기존 worktree와 `.tmp/recovery-20260915`는 복구용으로 보존한다. 별도 확인 없이 삭제하거나 그곳에서 기능 개발을 재개하지 않는다.
- 통합 근거와 검증 결과: `docs/INTEGRATION_HANDOFF_20260915.md`.
- 사용자가 요청한 범위만 수정하고, 기존 운영 데이터와 이력을 보존한다. 검사와 출력은 필요한 범위로 제한한다.

---

## 실제 작업 연결 완료 (2026-09-10)

사용자는 아래 기존 작업을 그대로 사용한다. 예전에 준비한 E:의 product/outbound 폴더가 기존 작업의 자동 연결 위치라고 안내하지 않는다.

| 담당 | Codex 작업 이름 | 실제 소스 수정 위치 |
| --- | --- | --- |
| 상품등록·이미지·견적서 | AI상품등록도우미 관리자 | C:\Users\noidb\.codex\worktrees\6281\노이드비AI |
| 입고·거래처·재발주 | 메뉴별 안전한 워크트리 분리 | E:\노이드비AI\.worktrees\inbound |
| 피킹·출고·송장 | 출고 서류 단계 안전 마무리 | C:\Users\noidb\.codex\worktrees\bc73\노이드비AI |

상품 작업은 Codex handoff 성공으로 실제 cwd가 변경됐다. 후속 작업 ID는 01a08b65-a602-7e10-b307-df04a6ec339c이며 기존 대화 기록이 이어져 있다. 출고 작업은 이미 독립 워크트리를 사용하며 기존 미커밋 수정은 그대로 보존한다.

이 입고 작업의 앱 등록 cwd는 원본 E:\노이드비AI로 남아 있으므로 거래처/입고 코드 명령은 반드시 .worktrees\inbound를 workdir로 지정한다. 이 작업의 소스 수정은 이미 그 폴더에서 진행했다. 원본은 연결 문서와 통합 조정에만 사용한다.

앱에서 여는 프로젝트 경로와 실제 작업 cwd를 확인한다. 한 작업 폴더에서 수정 작업은 하나만 실행한다. 새 메뉴 폴더를 다시 만들거나 기존 작업을 원본으로 되돌릴 필요가 없다. 운영 배포는 최신 운영 소스를 확인하고 해당 메뉴 차이만 합쳐 한 번에 하나씩 진행한다.

## 사용자 지정 기본 작업 규칙

- 현재 사용자가 요청한 작업에 필요한 파일만 확인한다.
- 프로젝트 전체를 반복해서 스캔하지 않는다.
- 관련 없는 폴더, 메뉴, 파일은 열지 않는다.
- 사용자가 요청하지 않은 리팩터링, 구조개선, 코드정리, 성능개선 작업을 임의로 하지 않는다.
- 이미 확인한 파일은 특별한 이유가 없으면 반복해서 다시 읽지 않는다.
- 현재 요청에 직접 관련된 파일부터 확인한다.
- 테스트는 현재 수정 범위에 필요한 것만 실행한다.
- 전체 빌드, 전체 테스트, 전체 린트는 꼭 필요한 경우에만 실행한다.
- 같은 테스트나 검증을 이유 없이 반복하지 않는다.
- 불필요한 브라우저 탐색, 플러그인 호출, 외부 도구 호출을 최소화한다.
- 긴 설명보다 실제 수정 작업을 우선한다.
- 작업 완료 보고는 수정 파일, 핵심 변경, 테스트 결과만 간단히 작성한다.
- 현재 워크트리 밖의 다른 워크트리와 main 브랜치는 수정하지 않는다.
- 사용자가 요청하지 않으면 main 병합, push, 운영 배포를 하지 않는다.
- 공통 파일 수정으로 다른 워크트리와 충돌 가능성이 있으면 먼저 알린다.
- 작업 하나가 완료되면 해당 작업 범위만 커밋한다.
- 현재 요청과 무관한 코드 조사, 구조 분석, 개선 제안은 하지 않는다.
- 토큰과 사용량 절약을 우선한다.

## 관리자창 승인 위임

- 이 관리자창의 워크트리 관리자가 사용자의 요청을 바탕으로 구체적으로 지시한 작업·대상·범위는 사용자의 승인으로 간주한다.
- 각 worktree는 그 지시를 해당 범위 안에서 실행하고 결과를 관리자창에 보고한다.
- 지시 범위를 벗어난 파일 수정, 삭제, 운영 데이터 변경, 배포는 승인된 것으로 간주하지 않는다.

## 워크트리 관리자 책임

- 관리자는 각 worktree에 작업을 분배하고, 답변·실행 결과·검증 결과를 직접 회수해 사용자에게 보고한다.
- 관리자는 worktree 결과를 바탕으로 자신이 처리할 수 있는 후속 조정·확인·정리를 최대한 수행한다.
- 사용자의 명시적 승인이 필요한 외부 변경, 운영 배포, 삭제, 전송, 결제성 작업만 사용자에게 승인 요청한다.
- 결과가 회수되지 않거나 실행이 시작되지 않으면 무기한 대기하지 말고 연결 오류·실행 상태·필요한 다음 조치를 즉시 보고한다.

## 시간·토큰 최소화 최우선

- 단순 운영·데이터 처리는 기존 화면/API로 먼저 실행한다.
- 실패 전 코드 수정·전체 분석·새 브랜치·임시 배포를 하지 않는다.
- 수정이 필요할 때만 최소 파일을 수정하고 필요한 테스트·배포만 한다.
- 같은 검증과 불필요한 탐색·설명을 반복하지 않는다.
