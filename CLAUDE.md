# 개발 규칙 (Claude Code용)

## 개발 중 표준 검증 절차

1. **UI 확인**: `npm run dev`
2. **타입 확인**: `npx tsc --noEmit` (개발 서버를 켠 채로 실행해도 안전 — `.next`를 건드리지 않음)
3. **최종 배포 검증**: `npm run build` — **반드시 개발 서버를 먼저 종료한 뒤에 실행한다**

**개발 서버 실행 중에는 `npm run build`를 실행하지 않는다.** 코드를 수정한 뒤 "문제없이
컴파일되는지"만 빠르게 확인하고 싶다면 1~2단계(`npm run dev` + `npx tsc --noEmit`)로 충분하며,
`npm run build`는 실제 배포 전 최종 검증이 필요할 때만, 그것도 개발 서버를 끈 뒤에 실행한다.

## Next.js 빌드 검증 시 `.next` 캐시 충돌 방지

`npm run dev`와 `npm run build`는 같은 `.next` 폴더를 공유한다. 개발 서버가 켜진 상태에서
프로덕션 빌드를 실행하면 웹팩 런타임 파일이 깨져 `Cannot find module './NNN.js'` 같은 오류로
개발 서버가 500을 내는 경우가 반복적으로 발생했다 (2026-08-18).

**반드시 아래처럼 검증 방식을 분리한다. 개발 서버가 실행 중일 때 `npm run build`를 동시에
실행하지 않는다.**

- **개발 중 화면 확인**: `npm run dev`만 사용한다.
- **프로덕션 빌드 확인**: 먼저 개발 서버를 종료한 뒤 `npm run build`를 실행한다.
- **빌드 확인이 끝나면**: `npm run dev`를 다시 시작한다.

### 만약 이미 충돌이 발생했다면 (`.next` 캐시 손상)

1. 오류 메시지가 `Cannot find module './NNN.js'` 류의 webpack 청크/런타임 참조 오류인지 확인한다
   (`.next/server/webpack-runtime.js` 관련 스택트레이스가 전형적 신호). 이는 소스코드 버그가
   아니라 캐시 손상이므로 소스코드를 의심하거나 수정하지 않는다.
2. 3000번 포트를 사용 중인 프로세스가 실제로 이 프로젝트의 `npm run dev`인지 먼저 확인한다
   (`netstat`로 PID 확인 → `CommandLine`이 `.../node_modules/next/dist/server/lib/start-server.js`인지 확인).
3. 확인된 그 프로세스만 종료한다. 종료 전에는 항상 사용자에게 먼저 보고하고 승인을 받는다.
4. `.next` 폴더를 삭제할지 여부도 사용자 승인을 받는다 (`.next`는 빌드 캐시일 뿐 소스코드가
   아니며, `npm run dev` 재실행 시 자동으로 다시 생성된다).
5. 승인 후 `.next`만 삭제하고 `npm run dev`로 재실행한다.
6. `/`, `/wms`, `/wms/work-center`, `/wms/picking` 등 주요 라우트가 모두 200인지 확인한다.
7. Cloudflare Tunnel 등 로컬 서버에 연결된 외부 터널이 있다면, 로컬 서버 복구 후에도
   정상 프록시되는지 함께 확인한다 (터널 프로세스 자체는 보통 재시작할 필요 없음).

## 그 외 사전 승인이 필요한 작업

다음은 실행 전 반드시 사용자 승인을 받는다: 프로세스 종료, 파일 삭제, 새 패키지/도구 설치,
Git 작업(`commit`/`push`/`reset` 등), 실제 외부 데이터 변경(구글시트 쓰기 등). 읽기 전용 검사와
빌드는 승인 없이 자유롭게 진행한다.
