# NOID-B 프로젝트 이어가기

이 프로젝트의 현재 작업 폴더는 `E:\노이드비AI`이다. 과거 다른 PC의 경로·커밋·배포 결과를 현재 상태로 간주하지 않는다.

## 2026-09-08 사용자가 확정한 업무 흐름

- 먼저 발주확정을 진행하고 입고생성내역의 실제 미입고분을 확인한다. 거래처 발주와 미납 재발주요청은 그 실제 수량을 근거로 진행한다.
- 주간업무에서 거래처 발주를 선택한 상품은 기존 거래처 발주대기와 함께 `/wms/vendor-orders/manage`에서 취합한다. 정확한 SKU로 중복을 제외하고 기존에 편집한 거래처·수량·메모를 유지한다.
- 주간업무에서는 발주수량 입력·수량 확인 체크·추가주문 제안이 필요 없다. 실제 미입고 수량을 초안으로 전달하고 기존 12개 단위 올림 규칙을 적용한다. 초안에서 수정·사진 보충·카카오톡 주문을 진행한다.
- 사진 복사·붙여넣기는 거래처 초안에서도 지원한다. 사진 저장 때문에 다른 기기의 수량·메모·입고이력을 덮어쓰면 안 된다.
- 저재고 0~1개 추가발주 추천 목록은 사용하지 않는다.
- 단종대기는 피킹에서 누적하고 바로가기로 확인한다. 주간업무의 단종 목록에서 함께 서류를 생성하고 실제 신청 완료를 표시한다.
- 쿠폰·광고 등록, 거래처 주문, 재발주요청, 단종 신청을 완료 표시한 항목은 진행 목록과 파일 선택 목록에서 제외한다. 다운로드만으로 완료 처리하지 않는다. 완료 이력은 보존한다.
- 메인 출고 작업 카드에 출고완료 버튼이 필요하다. 전체 Shipment 출고완료도 메인에서 반영하여 완료 작업을 진행 목록에서 숨긴다. 서류 또는 실제 포장 수량이 바뀌면 이전 자동 완료를 그대로 이어 쓰지 않는다.

## 작업 경계

- 이번 변경은 로컬 소스와 모의 UI/API 검증 후 2026-09-08 운영 배포 dpl_9Kj6QK2YNcC7p4u2EqrkpiiFXAza로 반영했다. 운영 주소 https://noidb-os.vercel.app 의 응답과 새 화면까지 확인했다. 운영 데이터에 완료 표시를 대신 저장하거나 카카오톡으로 실제 전송하지 않았다.
- 현재 배포 여부와 남은 검증은 `docs/WORKFLOW_REVISION_RELEASE_20260908.md`를 확인한다. 소스 수정이나 테스트 통과를 운영 배포 완료로 설명하지 않는다.
- 개발 서버 실행 중 `npm run build`를 실행하지 않는다. 서버 종료·파일 삭제·Git 변경·패키지 설치·운영 데이터 변경은 `AGENTS.md`의 승인 규칙을 따른다.
- 광고관리 작업은 별도 작업·폴더로 진행한다. 그 작업의 현재 상태는 여기서 추정하지 않는다.
- 대화 수집 범위는 `docs/chat-history/README.md`에서 확인한다. 요약만 보고 이전 프로젝트 원본·첨부파일을 삭제하지 않는다.

## 2026-09-08 교차 웨이브 완료 후속 수정

동일 발주번호라도 같은 입고일·센터·SKU·바코드·요청수량인지 확인하고, 다른 웨이브의 실제 출고완료 발주를 활성 작업에서 자동 제외한다. 목록만 숨기지 않고 피킹 수량·발주확정·송장·포장까지 같은 범위를 사용하며 원본·거래처발주·재출력 이력은 보존한다. 변경 시 앞뒤 단계와 중복 웨이브를 함께 검증한다. 최종 배포 dpl_4Pt3572ikgUsFUAfrs4z1yaG8xbs; docs/CROSS_WAVE_DISPATCH_FIX_20260908.md 참고.


## 2026-09-08 반복 검증 비용 개선

사용자는 오류를 직접 찾아 반복 테스트·수정·배포를 지시하는 부담을 줄이라고 요청했다. 같은 업무의 앞뒤 연결을 개발 측에서 함께 검증하고 모의 검증과 실제 외부 시스템 검증을 구분한다. 양식 미준비를 발주 오류로 표시하지 않는다. 우편번호 검색 결과와 원본 주소의 지역명 변경도 정확한 구·도로/지번·번호 기준으로 판단한다. 재발 방지는 verify:source-preparation과 기존 build 연결을 유지한다. 현재 자동화의 구현/미구현 범위 및 다른 도구 대안은 docs/OPERATIONS_AUTOMATION_AND_RELEASE_GATE_20260908.md에 기록했다. 새 도구 설치나 쿠팡 실제 업로드 자동화 완료로 오인하지 않는다.


## 2026-09-08 거래처 초안 일관성 보완

사용자 요청: 선택삭제 즉시 저장, 단종/미납 재발주 완료 자동 제외, 메인·주간업무·다른 창의 동일 초안, 승인 후 상품 추가, 정확한 모델 옵션 복수 추가, 거래처 선택 목록, SKU 78490107/78490105 모델의 이미지·색상·호수 보완.
구현/근거 문서: docs/VENDOR_DRAFT_CONSISTENCY_FIX_20260908.md.
기존 배포 dpl_CZBo9y7Wd9Tp2UTZRtVe8FmB69Li 보존, 최종 로컬 66단계 빌드 검증 통과. 390/1280 브라우저와 두 창·재접속·되돌리기 검증 통과.
새 배포 dpl_CtqfMgPrQ1xQ6fe98CuS4sE9GEuN READY 및 운영 API/화면 확인 완료. 운영 조회 전후 revision 694 동일, 테스트 쓰기 0. 최종 운영 확인 결과는 위 문서와 outputs/vendor-draft-fix-20260908/production-results.json에 기록한다.
중요: 재발주 부분 완료는 PO별 실제량 근거 없이 임의 차감/전체 삭제하지 않는다. DB 실제 이미지 없는 SKU는 자동 생성하지 않는다. 전송·입고 이력은 보존한다.
