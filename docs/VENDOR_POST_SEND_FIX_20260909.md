# 거래처 발주 후 처리 수정

- 단종대기 분류는 해당 SKU만 발주서에서 제외하고 단종대기에 저장한다. 분류 후 페이지는 유지하며 상단 단종·해제 관리에서 일괄 처리한다.
- 옵션 추가가 없는 카드에도 44px 슬롯을 남겨 하단 거래처·메모·상태 버튼 위치를 맞춘다.
- 승인·전송완료 발주서는 발주내용 수정으로 편집을 시작한다. 상품 추가, 옵션 추가, 삭제와 단종 분류로 잠금을 우회하지 못한다.
- 전송완료 거래처의 + 새 발주서는 이전 발주서를 이력으로 보존하고 빈 새 초안을 원자적으로 만든다. 이전 수량, 메모, 전송시각과 입고 기록은 보존하며 동시 생성 충돌을 차단한다.
- 공통 관리 화면에서 같은 화면으로 돌아가는 하단 링크를 제거한다.

검증: 격리 fixture에서 단종 SKU 2001 분류 후 화면 유지, 승인 잠금, 동일 거래처 신규 SKU 1002 추가·저장·재접속, 이전 SKU 1001 수량 10과 메모 보존, 모바일 가로 넘침 없음. 운영 시트 쓰기 0. 새 초안 추가 시 재전송 상태로 오표시되던 로컬 상태 처리도 수정했다.

배포 소스는 기존 운영 dpl_FXkP9cB4JrV65EwV8K426Ftp5W4P의 586개 파일을 해시 검증한 뒤 이번 관련 파일만 반영한다. 최종 빌드와 배포 결과는 outputs/vendor-post-send-20260909에 기록한다.

배포 완료: dpl_Gir8SMqqiwmE1Sh5bgrs9mK5t68F READY, noidb-os.vercel.app 연결 확인. 운영 4개 화면 HTTP 200, 정적 스크립트 19개 정상, 새 발주서 버튼과 옵션 슬롯 코드 제공 확인. 최종 격리 빌드 ID: fp9LmvoUvRskdMeq4ePz8.
