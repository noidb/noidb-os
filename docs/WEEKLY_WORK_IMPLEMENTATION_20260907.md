# 주간 입고 업무 변경 및 검증

작업 위치: `E:\노이드비AI` · 화면: `/wms/inbound` · 2026-09-07

## 사용 흐름

1. 처음에는 최근 한 달, 이후에는 최근 일주일을 선택하고 **이 기간 업무 준비**를 누른다.
2. 실제 입고일 기준으로 기간 전체를 분석한다. 쿠폰은 SKU당 한 줄이며 할인율은 30%다. 거래처 수량 검토 중에도 쿠폰 파일을 별도로 받을 수 있다.
3. 확인할 상품에서 누락 사진을 추가하거나 붙여넣고, 거래처와 주문수량을 검토한다. 사진과 거래처 지정은 다음 작업에 재사용한다. 표시된 상품의 수량을 일괄 확인할 수 있다.
4. 보류 상품은 다음 업무에 이월한다. 단종을 선택하면 발주에서 제외하고 단종 공문 PDF와 SKU 엑셀에 모은다.
5. 전체 파일 버튼으로 쿠폰 엑셀, 거래처별 사진 발주서 PNG와 엑셀, 단종 PDF와 엑셀을 받는다. 거래처 사진 발주서는 8개 상품씩 나눈다.
6. 실제 쿠팡 등록·거래처 발송·단종 신청을 마친 항목만 완료로 표시한다. 거래처 발송 완료는 기존 입고관리 발주 목록과 연결된다.

## 데이터와 반복 실행

- 쿠폰에는 선택 기간의 실제 입고 기록만 사용하고 반출 기록은 제외한다. 파일명이 달라도 동일 입고 이벤트는 중복 반영하지 않는다.
- 브라우저가 전체 조회를 완료한 경우 해당 기간의 보관 파일보다 브라우저 자료를 우선한다. 전체 조회 0건도 유효한 결과다.
- 확정수량 0, 취소, 미래 발주를 구분한다. 실제 부족수량과 기존 12개 단위 거래처 주문 제안은 별도로 표시한다.
- 대응 PO/SKU나 누적 입고·기존 주문의 근거가 부족하면 수량을 임의로 확정하지 않는다. 확인이 필요한 상품을 표시하고 양수 수량·사진·거래처 검토를 마쳐야 발주 파일을 만든다.
- 작업과 검토 변경은 별도 주간 업무 저장소에 저장한다. 동시 수정은 revision으로 검사한다. 발송 재시도는 같은 발주 ID를 사용하고 이미 입력된 입고수량을 덮어쓰지 않는다.
- 다른 주간 작업에서 이미 쿠폰 등록을 완료한 입고는 다시 제외한다. 열린 작업 사이에서 완료 순서가 바뀌어도 이전 파일로 중복 처리되지 않도록 생성·완료 단계에서도 검사한다.
- 최신 분석에서 발송·단종 처리된 PO+SKU는 옛 보류 기록 때문에 다시 이월되지 않는다.
- 파일 생성 표시와 실제 등록·발송·신청 완료는 별개다. 검증 과정에서 실제 발송이나 쿠팡 등록은 수행하지 않았다.

## 검증 결과

- `npx tsc --noEmit --incremental false` 통과.
- `npm run build`의 기존 WMS 회귀 검사와 신규 `verify:weekly-work` 통과.
- 마지막 이월 수정 후 해당 분석 fixture 및 TypeScript 재검증 통과. 최종 `npx next build`도 통과했다.
- 새 확장 1.3.0 ZIP 생성 및 전체 페이지·누락·기간·반복·건수 변화·빈 결과·전송 확인 검사 통과.
- 운영 API를 모두 가로챈 격리 브라우저 화면에서 사진 업로드, 자동 저장, 수량 일괄 확인, 보류, 쿠폰 독립 생성, 전체 7개 파일 생성, 각 완료 표시와 전체 완료까지 확인했다. API 운영 전달 건수는 0이었다.
- 데스크톱과 390px 모바일 화면을 확인했다. 모바일 document 가로폭과 scrollWidth는 모두 375px로 가로 넘침이 없었다.

실제 사용자 파일 `Coupang_Stocked_Data_List(2026-08-07~2026-09-07).xlsx`의 424개 기록을 읽어 아래 기간별 쿠폰 결과와 엑셀의 모든 할인율 30을 검증했다.

| 실제 입고일 기간 | 중복 없는 쿠폰 SKU |
| --- | ---: |
| 2026-08-07 ~ 2026-09-07 | 379 |
| 2026-09-01 ~ 2026-09-07 | 206 |
| 2026-09-07 | 39 |

실자료 확인에서는 Drive 원문 조회 실패와 일부 대응 발주이력 누락이 있어 거래처 주문수량을 최종 확정하지 않았다. 이 경우 화면에서 연결을 복구하거나 수량을 확인해야 한다. 기존 PO 파서는 확정수량 빈칸과 0을 구분하지 못하므로 이를 임의로 보강 근거로 사용하지 않았다.

## 적용 범위

2026-09-07 사용자 배포 승인 후 운영 사이트에 배포했다. 최종 배포는 `dpl_8rvN28qVa4kWAVd7ZrpwHVXQ1No7`, 상태는 READY이며 `https://noidb-os.vercel.app`에 연결됐다. 20:40 KST에 주간 업무 화면과 저장소 조회 API의 HTTP 200, 확장 ZIP 1.3.0의 로컬 파일과 동일한 해시를 확인했다. 실제 Chrome에서도 최근 한 달과 **이 기간 업무 준비** 버튼이 표시됨을 확인했다.

배포 입력의 `.vercelignore`는 기존 로컬 비밀 파일 제외를 유지하며, `/output/`, `/outputs/`, `/tmp/` 등은 루트 디렉터리에만 적용한다. 첫 클라우드 빌드에서 `output/`의 비한정 규칙이 API 경로까지 제외한 문제를 수정했으며, 최종 클라우드 빌드는 전체 회귀 검사를 통과했다. 로컬 `.next`를 올리지 않고 Vercel에서 소스 빌드했다.

설치된 브라우저 확장 업데이트와 설치된 확장을 통한 실제 전체 자료 전송은 수행하지 않았다. 브라우저 보안 정책이 확장 관리 화면 접근을 차단했다. 자동 수집을 사용하려면 기존 설치 폴더에 새 ZIP을 풀고 확장 및 사이트를 새로고침해야 한다. 파일 업로드는 확장 업데이트 없이 사용할 수 있다. 새 주간 화면에는 확장 ZIP과 Drive 재연결 안내가 있다. 운영 Sheets/Drive 변경과 실제 발송·쿠폰 등록·단종 제출은 검증 범위에서 제외했다.

기존 날짜별 입고 화면은 접힌 **이전 입고기록 화면 열기**에 보존했다. 기존 피킹·출고·바코드 로직과 관련 없는 사용자 작업 파일은 수정하지 않았다.

## 2026-09-07 후속 정정: 실제 입고 기준과 쿠폰 선택

위 배포·검증 기록은 최초 규칙의 이력이다. 사용자가 이후 확정한 아래 규칙으로 분석·선택 흐름을 수정했다. 위 실자료 쿠폰 수치 379/206/39개는 이전 규칙 결과이므로 새 규칙의 결과로 사용하지 않는다. 이 절은 후속 수정 내용이며 새 배포 완료를 의미하지 않는다.

- 쿠폰 후보는 선택 기간의 실제 입고일에 발생한 양수 입고수량을 SKU별·발주서별 기록 전체에서 합쳐 정확히 1개인 SKU만이다. 1개+1개 입고는 제외하며, 반출은 입고 합계에서 빼지 않는다.
- **쿠폰 적용 상품을 선택하세요**에서 개별 체크·전체 선택·전체 해제·SKU/상품명 검색을 제공한다. 적용·제외 건수를 표시하고 체크한 SKU만 30% 엑셀과 완료 상태에 반영한다. 전체 선택·해제는 검색 결과로 범위를 좁히지 않는다.
- 곧 단종되거나 생산 계획이 없는 SKU는 쿠폰 체크만 해제한다. 이 작업의 쿠폰 제외 선택이며 입고 데이터 삭제나 영구 단종 처리가 아니다. 실제 쿠팡 등록 완료를 기록한 뒤에는 등록 대상 선택을 고정한다.
- 미입고 후보는 선택 기간에 양수 실제 입고가 확인된 발주서로 한정한다. 과거 보류·미발송 이월도 양수 실제 입고 근거가 있는 발주서만 대조한다. 입고예정일이나 아직 전혀 입고되지 않은 발주서는 후보 선정 근거가 아니다.
- 대상 발주서의 SKU별 확정수량과 실제 누적 순입고(입고-반출)를 비교하고, 확정수량보다 적거나 0인 SKU의 부족분을 모은다. 전량 입고는 제외한다. 상품 카드에 **발주 확정 / 실제 누적 입고 / 쿠팡 미입고**를 나란히 표시한다.
- 이전 규칙으로 분석한 작업은 **쿠폰·미입고 기준이 변경되었습니다** 안내와 **이 기간 업무 준비**로 재분석을 유도한다. 기존 처리 기록은 보관한다.
- 사용 설명서 Markdown과 PDF를 v1.1로 개정했다. 쿠폰 선택과 미입고 기준을 전용 페이지로 분리해 총 6쪽으로 구성했다.

### 후속 검증과 운영 배포

- 최종 `npx tsc --noEmit`, `npm run build`, `git diff --check` 통과. 빌드에 포함된 주간 분석·상태·API·확장 검사와 기존 업무 검사도 통과했다.
- 격리된 브라우저 검증에서 쿠폰 개별 해제, 전체 해제, 재선택, 새로고침 후 선택 유지, 선택 1개 파일 생성, 미선택 시 쿠폰 버튼 비활성화를 확인했다. 브라우저 검증의 API는 임시 메모리 서버에서 처리했으며 운영 저장소에 쓰지 않았다.
- 실제 월간 엑셀 424건을 독립 계산한 SKU 집합과 앱 분석 결과가 일치했다. 한 파일 기준 입고 합계 1개인 SKU는 8/7–9/7 301개, 9/1–9/7 173개, 9/7 39개였다. 각 시험에서 SKU 1개를 제외한 XLSX에는 300/172/38개만 들어가고 전부 정률 30임을 파일 내부 XML로 확인했다. 다른 자료·기등록 내역·사용자 제외를 포함한 운영 총수는 아니다. 원본 변경과 운영 데이터 쓰기는 없었다.
- 2026-09-07 기존 배포 승인에 이어 후속 수정 배포 `dpl_8TTNvQeZ8Xqu7JLjt9CJFjX4n13b`가 READY가 되었으며 `https://noidb-os.vercel.app`에 연결됐다. 21:55 KST에 `/wms/inbound` HTTP 200과 입고 합계 1개 안내, 주간 업무 GET API의 HTTP 200/success를 확인했다. 실제 쿠팡 등록·거래처 발송은 수행하지 않았다.

## 2026-09-07 추가: 미납발주건 재발주요청

- 처리 방법에 **재발주요청 · 재고 있음**을 추가했다. 사진·거래처 입력 없이 보유 재고로 출고할 미납분을 선택하며, 원래 발주번호별 미납수량을 화면에 표시한다.
- 분석은 검증된 PO+SKU별 확정·누적입고·부족수량을 `shortageDetails`에 보존한다. 집계량을 여러 PO에 복제하거나 거래처 주문 제안의 12개 단위를 사용하지 않는다. 수량 원문 대조가 실패하거나 모호하면 재발주 출력을 차단한다. 실패 채널을 분석 식별자에 포함해 자료 복구 후 정상 분석으로 전환할 수 있다.
- `Report_Issue_Reorder.xlsx` 원본 사본을 앱에 포함했다. issueReport 시트 1행 식별값·2행 헤더와 나머지 패키지를 보존하고, 3행부터 SKU/원래 발주번호/정확한 미납수량/요청일/고정사유를 작성한다. 요청일은 한국 날짜 기준 생성일 다음 첫 금요일이며 금요일 생성 시 7일 뒤다. 사유는 `업체실수로 인한 출고누락`이다.
- 전체 파일 묶음과 재발주 단독 XLSX를 제공하며 실제 쿠팡의 미납발주건 수정발주요청 화면으로 연결한다. 파일 생성과 **쿠팡에 재발주 요청했어요** 완료 기록을 분리한다.
- 다른 작업에서 이미 요청한 PO+SKU는 새 요청에서 제외한다. 기요청과 신규 미납이 섞인 SKU는 기본 거래처 주문을 보류해 중복 구매를 막는다. 생성 날짜별 캐시와 생성/확인/완료 시 최신 자료·검토·요청 이력 검사를 적용한다. 기존 쿠폰 제외 선택과 등록·발송 기록을 보존한다.
- 검증용 브라우저에서는 사진 없는 SKU를 선택하여 두 PO의 5개·6개를 표시하고 9월 11일 예정일의 XLSX를 생성했다. 생성만으로 완료되지 않으며 명시적인 완료 후 선택이 잠겼다. 수량 근거 없는 SKU는 확인 필요 목록에 남고 다운로드가 차단됐다. 모든 API는 임시 메모리 서버에서 처리됐고 운영 API로 전달된 요청은 0이었다.
- 분석·상태·원본 XLSX·API 회귀검증과 TypeScript 검증을 통과했다. 사용 설명서는 재발주 전용 페이지를 추가한 v1.2, 총 7쪽으로 갱신했다. 실제 쿠팡 대량 가져오기/최종 제출은 수행하지 않았다.

### 재발주 기능 최종 검증·배포

2026-09-07 22:41 KST, 배포 `dpl_GdS5U2NTwwKj7ga47AB1ExA4HZ1s` READY 및 운영 별칭 `https://noidb-os.vercel.app` 연결 완료. 로컬 TypeScript·전체 빌드·diff 공백 검사와 클라우드 전체 빌드가 통과했다. 배포된 `/wms/inbound` HTTP 200/재발주 안내와 주간 업무 GET 성공을 확인했으며, 배포 전후 기존 4개 작업과 저장소·작업 revision은 변하지 않았다. 재발주 양식은 출력 API의 배포 추적 파일에도 포함되어 있다.

실자료 개수의 별도 로컬 재분석은 이 PC의 Drive OAuth 설정 3종 부재로 원본 읽기 전에 종료됐다. 운영 Drive 상태 API는 configured/connected true였으며 사용자 재연결을 요구하지 않았다. 이 로컬 점검으로 운영 SKU의 재발주 근거 개수·표본 합계까지 검증했다고 주장하지 않는다. 실제 쿠팡 대량 가져오기/최종 요청 제출은 수행하지 않았다.

## 2026-09-07 추가: 선택 쿠폰과 광고등록 파일 동시 생성

- 최종 선택한 쿠폰 SKU를 제품DB의 정확한 SKU ID에 연결하고, 옵션ID 열 또는 제품링크의 vendorItemId로 광고 옵션ID를 구한다. 노출상품ID는 사용하지 않는다. DB 값이 없을 때만 광고관리센터에서 직접 확인한 SKU와 제품링크 자료를 보충한다. 충돌은 임의로 결정하지 않는다.
- **쿠폰·광고 파일 함께 받기**는 기존 30% 쿠폰 양식과 광고 양식을 ZIP으로 제공한다. 사용자의 `상품대량등록.xlsx` 원본을 보존한 사본에서 Sheet1의 A열에 옵션ID만 채운다. 설명 셀과 나머지 파일 패키지는 보존한다.
- 광고 옵션ID를 먼저 중복 제거한 뒤 파일당 최대 500개로 나눈다. 파일명은 `3-1_광고등록.xlsx`, `3-2_광고등록.xlsx` 순서다. 501개는 500/1, 1,000개는 500/500, 1,001개는 500/500/1이며 빈 후속 파일은 생성하지 않는다. ID는 문자열로 보존한다.
- 쿠폰 체크를 해제하면 해당 SKU는 광고 대상에서도 빠진다. 미연결·충돌 ID가 있으면 화면에 대상 SKU를 표시하고 쿠폰·광고 동시 생성과 전체 묶음 생성을 차단한다. **쿠폰만 받기**는 별도로 제공한다. 상품 일부를 조용히 누락한 광고 파일은 생성하지 않는다.
- 미리보기·출력·생성 완료 기록 시 제품DB를 새로 읽고 선택과 매핑 변경을 재검사한다. 광고 매핑 토큰은 관련 출력 캐시에만 포함하며, 쿠폰만 받기와 거래처/단종/재발주 출력의 기존 캐시 규칙을 보존한다. 생성은 광고 등록 완료를 의미하지 않는다.
- 원본 양식과 0/1/499/500/501/1,000/1,001개 경계, 중복ID, 큰 정수ID 보존, 충돌/누락, 생성 중 선택·매핑 변경, 기존 출력 기능 회귀검사를 통과했다. 격리 브라우저에서 쿠폰 3개 중 1개를 제외해 2개의 쿠폰과 2개의 광고 옵션ID가 함께 내려받아지는 것을 확인했다. 브라우저의 운영 API 전달은 0건이었다.
- 최종 로컬 TypeScript 검사, 전체 `npm run build`, `git diff --check`를 통과했다. 광고 양식과 보충 매핑 JSON은 출력 API의 배포 추적 파일에 포함됐다. 매뉴얼은 광고 전용 페이지를 추가한 v1.3, 총 8쪽이다.
- 쿠폰 체크의 낙관적 화면 갱신과 저장 완료 사이에 이전 선택을 조회하는 경합을 최종 검토에서 수정했다. 광고 조회는 선택 저장 후 자동 시작한다. 독립 headless Chromium과 메모리 fixture에서 저장을 1초 지연해 초기 3개→1개 제외→쿠폰·광고 2개 자동 갱신을 확인했으며, 저장 중 광고 GET 0건·운영 API 전달 0건·화면 오류 0건이었다.

이 절의 배포 완료 기록은 운영 배포 검증 후 아래에 추가한다. 실제 쿠팡 광고 등록·광고 상품 선택·예산 설정은 수행하지 않았다.

2026-09-07 23:24 KST, 최신 운영 작업의 선택 쿠폰 301개와 제품DB 원문을 새로 대조했다. 기존 DB에서 비어 있던 44개는 로그인된 광고관리센터의 SKU ID 정확검색 결과에서 표시 옵션ID와 제품링크의 vendorItemId를 직접 확인해 보충 자료에 저장했다. 최종 연결 301개·중복 제거 후 옵션ID 301개·미연결 0개·충돌 0개였으며, 현재 선택 기준 광고파일은 `3-1_광고등록.xlsx` 1개다. 일시적인 쿠팡 검색 오류는 재진입 후 복구됐고 최종 미확인 SKU는 없다. 이 조회와 검증으로 운영 제품DB 또는 주간 작업 상태를 변경하지 않았다.

### 광고 기능 최종 검증·배포

2026-09-07 23:28 KST, 배포 `dpl_BvJF2V9xt7rbJKv6HvDCxFQWekv2` READY 및 운영 별칭 `https://noidb-os.vercel.app` 연결을 확인했다. 클라우드 전체 빌드도 통과했다. 배포된 주간 업무 페이지 HTTP 200, 새 쿠폰·광고 버튼과 파일명 안내, 광고 미리보기 API 성공을 확인했으며 운영에서도 선택 301개·연결 301개·미연결 0개·충돌 0개였다. 배포 전후 작업 5개와 저장소 revision 17 및 전체 작업 내용 해시가 동일했다.

현재 선택 자료의 사본으로 네트워크를 차단한 출력 검증을 추가했다. 쿠폰 XLSX의 301개 SKU·정률 30, 광고 XLSX의 301개 옵션ID가 각각 최종 선택과 동일한 순서로 일치했다. 이 검증은 파일을 메모리에서 생성·재해석했으며 운영 출력 캐시·등록 완료 상태·제품DB·Drive에 쓰지 않았다. 실제 쿠팡 쿠폰/광고 업로드·등록은 수행하지 않았다.

## 2026-09-08: 모바일 사용설명서 링크

PC 로컬 파일 링크 대신 `/downloads/noidb-weekly-work-manual-v1.3.pdf` 웹 주소로 8쪽 사용설명서를 게시하고 주간 업무 상단에 **사용설명서 PDF** 링크를 추가했다. 링크는 좁은 화면에서 줄바꿈되며 PDF는 새 창에서 열린다. 원본 PDF와 게시 파일의 SHA-256은 `91076b4541cdac5ab8ed4dda25eedb9f96980963b973cc453fa7d6d2e4f5180e`로 동일하다.

TypeScript·전체 빌드·diff 검사를 통과했다. 2026-09-08 00:06 KST, 배포 `dpl_dZsvyWQQKzVeKH1HPBcEVThvkmNw` READY 및 운영 별칭 연결 완료. 인증 없이 모바일 User-Agent로 PDF HTTP 200, application/pdf, inline 표시, 원본과 동일한 201,437바이트, 부분 읽기 HTTP 206을 확인했다. 운영 페이지에도 해당 링크가 표시된다. 실제 휴대폰 기기에서의 수동 검증은 수행하지 않았으며 운영 데이터 쓰기는 없었다.

## 2026-09-08: 업로드 발주서 연결과 확실한 미납만 표시

사용자가 업로드한 최근 한 달 발주서 ZIP을 주간 분석에서 직접 읽는다. `_발주이력`에 없는 행을 분석용으로만 보충하며, Sheet·Drive·재고·출고 상태를 갱신하거나 과거 한진 파일을 생성하지 않는다. 원문 H열 `업체납품가능수량`이 확정수량이며 명시적 0을 보존한다. 빈 확정수량, 수식·중복·합계 오류, 최신 원문 충돌은 이전 정상본으로 대신하지 않는다.

실제 입고가 시작된 발주서만 대조하고 원문 I열 입고수량으로 입고상세내역의 누적수량을 검산한다. 확정수량과 누적수량이 모두 확인된 양수 부족만 미납 카드와 거래처·재발주 파일 대상이다. 원문 미존재·연결 누락·수량 불일치는 `unresolvedItems`에 분리한다. 같은 SKU의 발주 중 하나라도 불확실하면 부분 합계만 노출하지 않는다. 쿠폰은 계속 실제 입고일 기준 기간 합계 1개이며 원문 I열을 쿠폰 입고 이벤트로 사용하지 않는다.

규칙 버전 3으로 이전 불확실 미납 분석의 출력과 상태 변경을 차단한다. 발주 파일 목록의 ID·이름·수정시각·크기를 분석 및 출력 최신성 토큰에 동일하게 반영하며, 분석 중 업로드 변경도 차단한다. 동일 기간 미완료 사용자 검토는 이어받되 수량 확인 체크를 해제하고, 완료 내역과 제거된 SKU의 검토는 새 업무에 복사하지 않는다.

00:48 KST 실물·읽기 전용 대조 결과: 새 ZIP 2개 81발주서 1,295상품행에서 파싱 오류 0. 기존 이력 누락 4발주서의 49상품행을 연결했다. 사진의 SKU 39136021·39323459는 PO 140245699에서 각각 확정 1·입고 1로 미납 제외됐다. 최근 입고 기간 2026-08-07~09-07 중 7월27일~8월3일 발행된 원문 미존재 8발주서는 별도 자료 확인으로 제외했다. 최종 원문 대조 가능 미납 63 SKU, 자료 확인 필요 78 SKU, 쿠폰 후보 301 SKU, 입고 원문 오류 0이었다. 검증 단계의 운영 쓰기는 0이다.

발주 파서·병합·source wrapper·상태·API 회귀는 원문 미존재/최신 오류/빈 H·I/동시각 충돌/기존 명시적 0/불확실 SKU 제외/토큰 일치/사용자 검토 계승을 검증한다. 최종 빌드·모바일 화면·운영 배포 결과는 아래에 추가 기록한다.

390×844 모바일 fixture에서 정상 확정 5·입고 3·미납 2 표시, 불확실 SKU의 전체/검색/카드 제외, 불확실 항목만 있을 때 오해 없는 빈 목록과 출력 비활성, 이전 규칙 2 분석 차단을 검증했다. 가로 넘침·페이지 오류·운영 API 전달/쓰기 0이었다. 설명서는 v1.4 PDF 9쪽(206,840바이트)으로 갱신하고 전 페이지를 렌더 검증했으며 기존 v1.3 파일을 보존했다.

최초 운영 검증에서 Google Sheets `_입고요약`의 일시적 조회 실패가 발생했고 모든 후보를 자료 확인 필요로 분리하는 동작을 확인했다. 추가로 일시적인 읽기 오류만 최대 3회 재시도하도록 보완했다. 자료 실패로 SKU가 미확인에만 남은 중간 분석은 검토 계승 시 건너뛰되, 정상적으로 목록에서 빠진 분석이나 완료 작업을 넘어 오래된 검토를 복원하지 않는다. 실제 운영 상태의 사본으로 미납 63·미확인 78 및 기존 사용자 검토 19건의 수량/선택 보존과 확인 체크 해제를 검증했다. 사본 검증에서 원래 업무와 제품별 저장 정보는 변경되지 않았다.

2026-09-08 01:13 KST 최종 운영 검증 완료. 배포 `dpl_EVukw6kTDmwU3ZC2pjpWzgY2vHuN` READY, 운영 별칭 `https://noidb-os.vercel.app` 연결 확인. 최종 코드의 TypeScript·전체 빌드·diff 검사와 클라우드 전체 빌드가 통과했다. 운영 재분석 작업 `WEEKLY-e947970d2df72ab7ce49`는 기간 2026-08-07~09-07, 규칙 3, 원문 보충 49행, 확정 미납 63 SKU·미확인 별도 제외 78 SKU였다. 쿠폰 후보 301 SKU 중 사용자가 제외한 7 SKU를 유지해 최종 선택은 294 SKU다. 기존 사용자 검토 19건을 이어받고 수량 확인 체크를 해제했다. 재분석 전의 모든 작업 내용과 제품별 저장 정보가 그대로 보존됐음을 확인했다. 새 주간 분석 상태만 저장했으며 Sheet·Drive·재고 변경과 쿠팡/거래처 전송은 수행하지 않았다. 모바일 User-Agent의 설명서 v1.4 HTTP 200 및 206,840바이트 원본 일치, 운영 화면의 새 설명서 링크를 확인했다.
