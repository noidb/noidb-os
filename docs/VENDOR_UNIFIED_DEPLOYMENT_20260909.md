# 거래처 발주 통합 수정 · 2026-09-09

이 문서는 앞선 VENDOR_UNIFIED_ENTRY_CHANGE_20260909.md의 후속 과제를 반영한 최종 수정 기록이다.

## 변경

- 거래처 발주 처리 진입을 /wms/vendor-orders/manage로 통일. 수동 취합 버튼 제거, 기존 미전송 발주 자동 연결. 예전 주소는 관리 화면으로 이동하고 history=1인 전송 이력만 열람 허용.
- 같은 SKU를 서로 다른 거래처로 보낸 경우 보존. 같은 거래처 안의 중복은 기존 방어 유지.
- 거래처 전송 후 공용 큐 ID 유지. 같은 거래처에 새 발주가 오면 이전 전송 발주에 archivedAt을 표시하고 새 초안을 추가. 전송 라인과 입고 수량·이력은 보존.
- 거래처별 임시저장·승인·전송은 해당 거래처만 저장. 전체 변경 저장도 변경된 거래처만 대상. 다른 거래처의 미저장 입력 보존.
- 10초 간격과 화면 복귀 시 최신 발주 반영. 원격 변경과 내 변경이 다른 필드면 병합, 같은 필드 또는 삭제와 겹치면 입력 보존 후 해결 선택. 오래된 조회 응답은 무시.
- 병합 후 같은 수정 시각 때문에 서버가 기존 수량을 유지하던 오류 수정: 저장 시각을 기준 데이터보다 크게 설정하고 실제 서버 저장값 검증.
- 신규 목록 요청·응답 본문 30초, 완료 확인 15초, 발주 저장 45초 제한. 실패해도 입력 유지.
- 주간업무의 중복 발주·단종 파일 생성/완료 UI 제거. 단종관리로 보내기는 정확한 공용 요청 ID를 연결하고 재시도 시 재사용. 공용 목록으로 보낸 것은 외부 처리완료로 간주하지 않음. 기존 완료 연결 재시도 경로는 보존.
- 단종관리 목록은 15초 및 화면 복귀 시 갱신. 상품 이미지 조회 때문에 목록 전체가 기다리지 않도록 분리.

## 검증

- npx --no-install tsc --noEmit --incremental false 통과.
- 별도 배포 폴더에서 npm run build 전체 사전검증·타입·프로덕션 빌드 통과. 최종 BUILD_ID: z1N7Wm7QYEpUeA83b-jVE.
- 신규 검증: verify-vendor-unified-entry, verify-vendor-device-merge, verify-weekly-discontinue-transfer. 기존 발주, 삭제, 완료 제외, 사진, 전송 파일, 큐 충돌·안정성, 주간업무 검증 포함.
- 모의 API 프록시를 통한 실제 브라우저 두 화면: 다른 기기 수량 반영 + 로컬 미저장 메모 보존, 같은 수량 충돌 안내, 내 입력 선택 후 실제 저장소 100개 반영.
- 최종 프로덕션 빌드의 모의 화면: 모바일 390/PC1280 가로 넘침 없음. A20 전송완료 후 B10 초안 유지. A의 신규30 추가 시 공용 큐 ID 유지, 이전 전송20 및 B10 보존.
- 테스트는 메모리 모의 데이터. 실제 발주·구글시트 쓰기, 카카오톡 메시지 전송은 테스트하지 않음.

## 배포 기록

- 기준 운영 배포: dpl_4p8TnVKfuvjnBNVzwtMo4vygfNn5. 이전 상품등록·주간업무 배포 입력 578개 해시 대조 후 이번 범위만 추가.
- 이번 배포 ID: dpl_4MejopmismMpkioZK5hSi5i3vcdm.
- Vercel READY 및 noidb-os.vercel.app 운영 별칭 연결 확인. 발주관리·단종관리·주간업무 HTTP200, 기존 허브의 관리 화면 이동 지시, 최신 화면 스크립트 포함 12개 자산 HTTP200 확인.
- 배포 폴더: outputs/deploy-stage-vendor-unified-20260909-final.
- 검증·업로드 기록: outputs/vendor-unified-20260909. 실제 배포 READY/운영 주소 확인은 해당 기록의 production-verification.json 참조.
- 기존 개발 서버와 캐시를 건드리지 않고 별도 폴더에서 빌드. 프로세스 종료의 자동 승인 거절 후 기존 프로세스는 유지.
