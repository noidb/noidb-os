# 다른 웨이브에서 출고완료한 발주서 자동 제외

## 확인한 문제

사용자가 출고완료한 발주서가 `WAVE-20260908-8af1667d`(9월 11일입고)에 미완료로 표시됐다. 운영 revision 632를 읽기 전용으로 확인했다.

- 완료 원본: `WAVE-20260908-80615522`, 사용자 수동 출고완료.
- 중복: `141427163`(인천36, SKU 1개, 수량 1개), `141427351`(이천4, SKU 3개, 수량 3개).
- 원인: 완료 상태가 웨이브 단위로만 연결되어 다른 웨이브의 동일 발주서에 적용되지 않았다.
- 수정 후 작업 대상: **10건 / 98 SKU / 152개 / 9센터 / 2026-09-11**.

## 구현 경계

발주번호·입고예정일·센터·SKU·바코드·원본 요청수량을 대조해 동일 발주인지 판단한다. 다른 작업의 명시적인 수동 출고완료 또는 현재 출력세대의 유효한 Shipment 출고완료만 근거로 삼는다. 확정수량만큼 출고하고 미입고분을 나중에 재발주하는 경우도 지원한다. 단순 피킹완료나 서류 생성만으로 제외하지 않는다.

작업센터, 기존 웨이브 목록, 피킹, 발주확정, 송장, 포장에는 남은 발주와 수량만 전달한다. 완료·미완료 PO가 같은 SKU에 섞여 있어도 미완료 수량을 유지한다. 통합 원본 파일에 완료 PO가 남아 있어도 카드에 다시 붙이지 않는다.

원본 발주·피킹·출력파일·거래처 발주 이력은 보존한다. 화면용 필터 결과는 공용 저장소나 로컬 복구 캐시의 원본을 대체하지 않는다. 필터된 부분의 저장만 원본에 병합하고, 다른 기기에서 완료·복원 상태가 바뀐 오래된 화면의 저장은 거절한다. 출고완료 취소 시 원본에서 자동 재표시된다.

기존 출력 묶음에 완료 PO가 섞여 있으면 그 파일을 남은 발주용으로 재사용하지 않는다. 남은 발주로 새 묶음을 생성하고, 원래 파일·PO 구성을 보존한 채 새 묶음과 연결한다. 새 완료 기록에는 그때 실제 작업한 PO 범위만 저장한다.

## 검증

- 교차 웨이브 완료 판정 29개 회귀검증.
- 활성 수량 계산·원본 이력 보존·실제 서버 저장·오래된 클라이언트 방지·완료 취소·출력 묶음 교체 검증.
- 발주확정/선택발주확정/송장/Shipment 생성 API 네 곳의 완료 PO 차단 검증.
- 실제 운영 자료를 메모리 복사본으로 사용해 문제 화면과 피킹 화면의 PC 1280px / 모바일 390px 확인. 완료 2 PO가 미표시되고 10/98/152로 집계됨. 완료 취소 시 12/102로 복원됨.
- 메인·기존 웨이브 목록 PC/모바일 확인: 10/98/152/9센터/9월 11일. 완료 원본은 완료·보관에서 유지.
- 통합 원본 12 PO를 다시 불러오는 응답에서도 완료 2 PO 미표시 확인.
- 검증은 운영 GET과 모의 저장만 사용했다. 실제 주문, 완료 표시, 쿠팡 등록, 카카오톡 전송은 수행하지 않았다.

증거: `outputs/cross-wave-dispatch-20260908/`의 `target-browser-results.json`, `summary-browser-results.json`, `final-build-results.json`.

## 배포 상태

2026-09-08 18:09 KST 운영 배포 완료: dpl_4Pt3572ikgUsFUAfrs4z1yaG8xbs, READY 및 noidb-os.vercel.app 연결 확인. 로컬 최종 build 53단계와 Vercel npm run build 통과. 운영 화면 확인 기록은 outputs/cross-wave-dispatch-20260908/production-results.json.
